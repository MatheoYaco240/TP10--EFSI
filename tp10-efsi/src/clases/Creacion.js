class Creacion {
    constructor(id, nombre, descripcion, imagen) {
      this.id = id;
      this.nombre = nombre;
      this.descripcion = descripcion;
      this.imagen = imagen;
    }
    /* // Getter
    get proyecto() {
      return this.calcArea();
    }
    // Método
    calcArea() {
      return this.alto * this.ancho;
    }*/
  }
  
  //const cuadrado = new Creacion(10, 10);
  
  //console.log(cuadrado.area); // 100